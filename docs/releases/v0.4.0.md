# Release v0.4.0

Release date: 2026-07-28

## Highlights

- Added a bounded LangGraph agent loop: inspect → plan → act → verify → replan.
- Added opt-in, path-scoped, read-only DeepAgent review for queued LLM candidates.
- Added targeted LLM authoring controls with `--only` and `--limit`.
- Improved lint handling for code examples and note names containing numeric dots.
- Normalized the tracked OKF registry to lowercase `index.md`.

## Verification

- One real vault note was compiled with one approved LLM authoring call.
- Semantic links and QMD were disabled for the focused AI test.
- Agent verification completed with zero lint errors.
- Focused test suite passed; the full suite retains one environment-sensitive query timeout assertion.
